# SimpleTier1 with the BioToFPS rows assigned to species group 98, while every
# species still resolves to group 99. This is the shape of the problem when a
# species is absent from SpeciesGroupTable: SpeciesGroupList.Find() returns 99
# for anything unlisted, and if no group 99 row exists the lookup finds nothing.
#
# RECORDS CURRENT BEHAVIOUR, which is wrong: FindForestMillList() returns null,
# the live harvest carbon is discarded, and the run still reports success. The
# only trace is two lines in FPS_log.txt.
exit_zero: yes
log_contains: No proportions found for Forest To Mills
log_contains: Allocated <> Available in Forest To Mills
